<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <permission
        android:name="com.redwood.permission.SECURE_IPC"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.redwood.permission.SECURE_IPC" />

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" tools:ignore="QueryAllPackagesPermission" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher" 
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.DynamicIsland">
        
        <service android:name=".DummyListener"
                 android:label="Dynamic Island Media Listener"
                 android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
                 android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="A Native Jetpack Compose Dynamic Island Overlay" />
        <meta-data
            android:name="xposedminversion"
            android:value="93" />
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposed_scope" />

        <activity
            android:name=".ui.ConfigActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".BootReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".IslandTileService"
            android:label="Island Config"
            android:icon="@mipmap/ic_launcher"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- ============================================================
             BATCH 4: AndroidManifest.xml additions
             ============================================================ -->

        <!-- Required for DeepTelecomHook contact photo loading in SystemUI -->
        <uses-permission android:name="android.permission.READ_CONTACTS" />

        <!-- Required for IslandAccessibilityService -->
        <service
            android:name=".accessibility.IslandAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>

    <!-- Required so SystemUI can resolve contact photo URIs -->
    <queries>
        <provider android:authorities="com.android.contacts" />
        <provider android:authorities="contacts" />
    </queries>

    <!-- ============================================================
         BATCH 1: AndroidManifest.xml additions
         ============================================================ -->

    <provider
        android:name=".ipc.IslandContentProvider"
        android:authorities="com.example.dynamicisland.provider"
        android:exported="true"
        android:readPermission="com.redwood.permission.SECURE_IPC"
        android:writePermission="com.redwood.permission.SECURE_IPC"
        android:multiprocess="false"
        android:syncable="false" />

    <service
        android:name=".ipc.IslandStateService"
        android:exported="true"
        android:permission="com.redwood.permission.SECURE_IPC">
        <intent-filter>
            <action android:name="com.example.dynamicisland.STATE_SERVICE" />
        </intent-filter>
    </service>

</manifest>